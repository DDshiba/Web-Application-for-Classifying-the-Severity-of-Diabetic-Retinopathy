services:
  - type: web
    name: fastapi-app
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn -w 1 -k sync -t 600 backend.app:app
    envVars:
      - key: PORT
        value: 10000
    plan: free
