services:
  - type: web
    name: fastapi-app
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
    plan: free
